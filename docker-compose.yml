version: '3.8'

services:
  mongodb:
    image: mongo:6.0
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_DATABASE=chess_analyzer
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
